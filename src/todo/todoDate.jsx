/* 
import React from 'react'

export default props => {
    const newDate = () => {
        var novaData = props.newdate;
        var dia = novaData.getDate();
        var mes = novaData.getMonth();
        var ano = novaData.getFullYear();

        return (
            <div>{novaData}</div>
        )
    }
} */